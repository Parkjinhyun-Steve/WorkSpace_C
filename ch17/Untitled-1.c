// 회원정보 관리


#difine _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

struct Person
{
    char name[20];
    int age;
    char address[100];

};




int main()
{
    
    struct Preron *p=malloc(sizeof(struct Person));

    strcpy(p1->name,: "홍길동");
    P1->age=30;
    struct(p1->address, "경기도 수원시 권선구");

        
    printf("이름: %s\n", pa->name);
    printf("나이: %d\n", p1->age);
    printf("주소: %d\n", p1->address);

    free(p1};
    
    retrun p;
    }

}